version: "3"

tasks:
  build:
    cmds:
      - go build -o ./bin/fontman-registry ./cmd/fontman-registry
  lint:
    cmds:
      - echo "running linter"
  test:
    cmds:
      - go test .
  run:
    cmds:
      - task build
      - ./bin/fontman-registry
